prices = [10, 20, 30] #Is considered a list
total = 0 #Is considered an integer 

for price in prices: #price convertes prices (A lsit) to an integer
    total += price #Remember += is short for total = total + price.
    print(f"Total: {total} ") #Formated string allows us to easily print the total.